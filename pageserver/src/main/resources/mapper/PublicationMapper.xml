<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.opennlg.mapper.PublicationMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.opennlg.pojo.Publication">
        <id column="publication_id" property="publicationId" />
        <result column="publication_title" property="publicationTitle" />
        <result column="publication_desc" property="publicationDesc" />
        <result column="publication_cover" property="publicationCover" />
        <result column="outside_url" property="outsideUrl" />
        <result column="inside_url" property="insideUrl" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        publication_id, publication_title, publication_desc, publication_cover, outside_url, inside_url, create_time, update_time
    </sql>
    <select id="selectListByPage" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from og_publication
        order by create_time desc
    </select>
</mapper>
